# 草诀歌 AI Labs — 设计规范

> 以产品为笔，和世界对话。

**这份文档给人看**（设计、开发、写文案的人），是查表用的。
`README.md` 是给 AI agent 看的英文长文，`SKILL.md` 是技能入口，`colors_and_type.css` 是唯一的 token 源头。三者内容一致，出现分歧时以 **CSS 文件为准**。

---

## 1. 一句话

草诀歌 AI Labs 是一个**面向非技术创作者的中文 vibe coding 社区**。
品牌名取自学草书的口诀歌 ——「**Vibe coding 是我们这个时代的草诀歌**」：让非专家做出专家级的东西。

视觉上因此不是「科技公司」，而是**编辑气质的印刷物**：灰度、留白、细线、衬线体。参照《巴黎评论》或纽约时报评论版，不是 SaaS 落地页。

---

## 2. 五条设计原则

| # | 原则 | 具体含义 |
|---|---|---|
| 1 | **对比靠明度，不靠颜色** | 全站没有品牌色。四个灰 + 黑 + 白，层级只靠深浅和字体切换。 |
| 2 | **1px 细线就是品牌** | 分隔、边框、章节交界全用 `rgba(71,73,75,.2)` 的发丝线。线是缝隙，不用 gap。 |
| 3 | **绝对平面** | 无阴影、无渐变、无毛玻璃、无圆角。想加阴影时，改成加一条细线。 |
| 4 | **衬线／无衬线的切换是主要层级手段** | 标题用 Noto Serif SC，正文 UI 用 Inter。字号压得很紧，靠换字体拉层级。 |
| 5 | **中英双语的节奏是质感** | 中文主体 + 英文全大写小标签（eyebrow）。英文标签不是装饰，是排版结构，不要删。 |

---

## 3. 色彩

只有 4 个灰 + 黑 + 白。**没有强调色，不要新增色相。**

| Token | 值 | 用在哪 |
|---|---|---|
| `--c-bg` | `#e5e5e5` | **页面底色**。品牌的底是暖灰，不是纯白。 |
| `--c-surface` | `#ffffff` | 输入框、浮起的卡片 |
| `--c-surface-40` | `rgba(255,255,255,.40)` | 活动卡半透明填充，在暖灰上轻微浮起 |
| `--c-ink` | `#1a1a1a` | 正文墨色 |
| `--c-ink-pure` | `#000000` | 极少用：按钮 hover、选中态 |
| `--c-btn` | `#47494b` | 按钮、深色区块（CTA 带 + 页脚 + 角色卡默认态） |
| `--c-border` | `#575757` | 强分隔线（少用） |
| `--c-border-soft` | `rgba(71,73,75,.20)` | **发丝线，全站到处在用** |
| `--c-muted` | `#5c5e60` | 次级正文 |
| `--c-muted-alt` | `#8b949e` | 三级文字、图注 |
| `--c-on-dark` | `#e5e5e5` | 深色区块上的文字 |
| `--c-watermark` | `rgba(0,0,0,.05)` | 巨大衬线水印字 |

规则：
- **深色区块上的字是 `#e5e5e5`，不是白色。** 不要白底黑字反过来写成纯白。
- 选中态是品牌的一部分：`::selection { background:#47494b; color:#e5e5e5; }`
- 深色块上的边框用 `rgba(255,255,255,.20)`。

---

## 4. 字体

**只有两个字族。**

| 用途 | 字族 | 说明 |
|---|---|---|
| 展示、引文、角色徽章、logo 里的「草诀歌」 | **Noto Serif SC** | `--font-serif` |
| UI、正文、导航、按钮 | **Inter** | `--font-sans` |
| 活动卡的小标签 | JetBrains Mono | `--font-mono`，仅此一处 |

### 字号

| Token | 大小 | 用途 |
|---|---|---|
| `--fs-display-xl` | 48 → 96px | 首屏主标题 |
| `--fs-display-l` | 32 → 60px | 章节标题 |
| `--fs-display-m` | 28 → 40px | 次级标题 |
| `--fs-lg` | 20px | 导语段 |
| `--fs-base` | 16px | 正文、按钮 |
| `--fs-sm` | 14px | 导航、meta |
| `--fs-xs` | 12px | 英文大写标签 |

### 字距（重要）

- 展示标题收紧：`-0.02em`
- 英文大写标签放到最开：`0.2em`（`--ls-widest`）
- 「AI LABS」锁定组合也用 `0.2em`

### 用衬线体的时机

- **衬线斜体是表达手段，不是默认。** 只用在标题的情绪落点（如「*和世界对话*」）、引文、三个角色徽章。
- 序号 `01 02 03` 用衬线体 —— 它们是排版装饰，不是数据。
- 中文正文段落内**不加粗**，重点靠衬线斜体或版式本身。

---

## 5. 间距 / 圆角 / 描边 / 阴影

| 项 | 规则 |
|---|---|
| 章节纵向留白 | `96px`（`--space-24`）是默认值，不要更紧 |
| 首屏高度 | `min-height: 85vh` |
| 容器 | 最大 `1280px`，左右 `24px` / 大屏 `48px` |
| 卡片内边距 | `32px` → 大屏 `48px` |
| 网格 | **边到边，不留 gap**，靠 1px 线分隔 |
| 圆角 | `0`。唯一例外：三个角色徽章用全圆 pill |
| 描边 | `1px solid rgba(71,73,75,.2)` |
| 阴影 | **没有。** token 里的 `--shadow-*` 是兜底，正常不要用 |
| 背景 | 纯色。**不要渐变、不要径向光**。唯一装饰是巨大半透明衬线字 |

---

## 6. 动效

克制到只有一个入场动画 + 一个炫技交互。

| 名称 | 参数 | 用在哪 |
|---|---|---|
| `fade-in-up` | `translateY(20px)` → 0，800ms，`cubic-bezier(.16,1,.3,1)` | 首屏内容，按 0/100/200/300ms 错开 |
| hover 变色 | 300–500ms，只变 `color` / `background` | 按钮、链接 |
| 角色卡反转 | 500ms：深底浅字 → 透明底深字 | 全站唯一的「炫技」交互 |
| 箭头位移 | `translate-x-1` | 唯一允许的微位移，只给箭头图标 |

**不要 transform，不要 scale，不要弹跳。**

---

## 7. 组件规则

### 导航
固定顶部，高 80px，实色 `#e5e5e5` 底 + 1px 底线。**不要毛玻璃。**

### 按钮
- 主按钮：`#47494b` 底，hover 变 `#000`（同色系更深）
- 次按钮（ghost）：透明底 + 细线，hover 填充成 `#47494b`、文字反白
- 直角，无阴影，无圆角
- 无显式 `:active` 态

### 链接
hover 变 `#47494b`，**不加下划线**。

### 卡片
`rgba(255,255,255,.40)` 填充 + 1px 细线，直角。内边距 32→48px。

### 角色卡（草书者 / 行书者 / 楷书者）
默认 `#47494b` 实底 + 1px 细线 + 内嵌巨大水印字（草/行/楷，`white/5`），hover 整体反转。

### 章节标题
统一格式：**英文全大写 eyebrow + 中文反问句标题**。
现有的四个：`社区相信什么？` / `社区做什么？` / `你可以获得什么？` / `社区里现在有谁？`
新增章节沿用这个句式。

### CTA 带 + 页脚
共用页面底部的一整块深色区（`#47494b`），文字 `#e5e5e5`。

---

## 8. 文案与内容

### 语言
- **中文为主。** 英文只作大写小标签和 logo 组合用。
- 品牌名写作 `草诀歌 AI Labs`，中间有空格。logo 里「草诀歌」是衬线 + 宽字距，「AI LABS」是无衬线 + 小号 + 大写 + 灰。

### 语气
- 文学的、有主张的、略带哲思。写给 **我们 / 你**，不写「用户 / 客户」。
- **善用隐喻。** 三个身份用书法字体命名：
  - `跨界者 / 草书者` —— 自由、有经验、乐于分享
  - `造物者 / 行书者` —— 在动手、在造东西
  - `探索者 / 楷书者` —— 在学习、讲求精确

  **不要**退化成「初级 / 中级 / 高级」。
- 「**美第奇效应**」是反复出现的概念：画家、科学家、工程师互相授粉。描述社区活动时用它。

### 必须原样保留的句子
- `以产品为笔，和世界对话`
- `Vibe coding 是我们这个时代的草诀歌`
- `美第奇效应`
- `AI 的真正红利不在批量制造垃圾，而在于创造独特作品`

### 标点
中文句子用全角 `，。？！`，**不要半角**。中文不做全大写。

---

## 9. 图标

- 默认用 **Lucide**（经 Iconify CDN 加载）。描边 2，圆头，不填充，`currentColor`。
- 内联 CTA 用 16–20px，独立图标 24px。
- **有 Lucide 现成图标时不要手画 SVG。**
- `草 / 行 / 楷` 是排版装饰，不是图标 —— 衬线体、巨大、5% 不透明度。

---

## 10. 禁忌清单

| ✗ 不要 | ✓ 改成 |
|---|---|
| emoji（任何位置） | 什么都不加，或用 lucide 图标 |
| 渐变背景 | 纯色 |
| 阴影 | 1px 细线 |
| 圆角卡片 | 直角 |
| 毛玻璃导航 | 实色 + 底线 |
| 新增品牌色 / 强调色 | 用明度做对比 |
| 中文加粗 | 衬线斜体 |
| 半角标点混在中文里 | 全角 |
| 中文全大写化 | 只给英文标签大写 |
| 「初级/中级/高级」 | 草书者 / 行书者 / 楷书者 |
| gap 分隔网格 | 1px 细线分隔 |
| hover 时缩放、位移 | 只变颜色（箭头除外） |

---

## 11. 交付前自查

- [ ] 页面底色是 `#e5e5e5` 而不是 `#fff`
- [ ] 深色块上的文字是 `#e5e5e5` 而不是纯白
- [ ] 所有分隔线是 `1px rgba(71,73,75,.2)`
- [ ] 全站零阴影、零渐变、零圆角（角色徽章除外）
- [ ] 只用了 Noto Serif SC + Inter
- [ ] 每个章节都有「英文 eyebrow + 中文反问句」
- [ ] 全角标点，无 emoji
- [ ] 章节纵向留白 ≥ 96px
- [ ] 有且只有一处水印字装饰，半裁切

---

## 12. 文件索引

| 文件 | 内容 |
|---|---|
| `设计规范.md` | 本文档 —— 中文查表规范 |
| `README.md` | 英文完整说明（agent 用） |
| `SKILL.md` | 技能入口 |
| `colors_and_type.css` | **所有 token 的唯一源头** |
| `reference/` | 原始素材：设计上下文 V1.0、主页 V1.0 |
| `preview/` | 设计系统面板的展示卡片 |
| `ui_kits/website/` | 主页的 React 组件复刻 |
| `assets/` | logo、图片、占位图 |

---

## 待补

- 尚无授权品牌字体，Inter / Noto Serif SC 走 Google Fonts CDN。若有正式授权字体，放入 `fonts/` 并更新 `colors_and_type.css`。
- 尚无 logo 文件，logo 目前是纯排版组合。
- 除主页外还没有其他界面。可预期的下一批：活动页、成员主页、申请状态页、社区内容流。
